Parallel arrays are when you have separate arrays for things that can be put into one array of a better object.
They are an indication of poor programming because it is easier to mess up and is not concise.
They can be avoided by creating an object that stores multiple data fields.